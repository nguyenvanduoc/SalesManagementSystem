@model SalesManagementSystem.Models.ViewModels.PagedListViewModel<SalesManagementSystem.Models.ViewModels.PhieuXuatKhoListViewModel>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-body p-0">
        <div class="table-responsive" style="min-height: 350px;">
            <table class="table table-custom table-bordered table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th class="text-center" style="width: 60px;">Thao tác</th>
                        <th class="text-center" style="width: 50px;">STT</th>
                        <th>Số chứng từ</th>
                        <th class="text-center">Ngày xuất</th>
                        <th>Kho xuất</th>
                        <th>Người nhận</th>
                        <th class="text-end">Tiền hàng</th>
                        <th class="text-end">Tiền thuế</th>
                        <th class="text-end">Tổng cộng</th>
                        <th>Người tạo</th>
                        <th class="text-center">Ngày tạo</th>
                        <th class="text-center">Trạng thái</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Items != null && Model.Items.Any())
                    {
                        int stt = (Model.CurrentPage - 1) * Model.PageSize + 1;
                        foreach (var item in Model.Items)
                        {
                            <tr>
                                <td class="text-center align-middle">
                                    <div class="dropdown dropdown-hover">
                                        <button class="btn btn-sm btn-light border dropdown-toggle-hide-arrow shadow-sm" type="button" data-bs-toggle="dropdown" aria-expanded="false" style="padding: 2px 6px;">
                                            <i class="bi bi-grid-3x3-gap-fill text-dark"></i>
                                        </button>
                                        <ul class="dropdown-menu shadow">
                                            <li><h6 class="dropdown-header text-uppercase fw-bold text-dark">Thao tác</h6></li>
                                            <li><hr class="dropdown-divider"></li>
                                            @if (SalesManagementSystem.Helpers.PermissionHelper.HasPermission("PhieuXuatKho", SalesManagementSystem.Helpers.LoaiPhanQuyen.Xem))
                                            {
                                                <li>
                                                    <a href="@Url.Action("Edit", "PhieuXuatKho", new { id = item.ID, isView = true })" class="dropdown-item">
                                                        <i class="bi bi-eye me-2 text-info"></i> Xem chi tiết
                                                    </a>
                                                </li>
                                            }
                                            @if (item.TrangThai == 1 && SalesManagementSystem.Helpers.PermissionHelper.HasPermission("PhieuXuatKho", SalesManagementSystem.Helpers.LoaiPhanQuyen.CapNhat))
                                            {
                                                <li>
                                                    <a href="@Url.Action("Edit", "PhieuXuatKho", new { id = item.ID })" class="dropdown-item">
                                                        <i class="bi bi-pencil-square me-2 text-primary"></i> Sửa
                                                    </a>
                                                </li>
                                                <li>
                                                    <button type="button" class="dropdown-item text-success" onclick="ghiSoPhieuXuat(this, @item.ID)">
                                                        <i class="bi bi-check-circle me-2 text-success"></i> Ghi dữ liệu
                                                    </button>
                                                </li>
                                            }
                                            
                                            @if (item.TrangThai == 2 && SalesManagementSystem.Helpers.PermissionHelper.HasPermission("PhieuXuatKho", SalesManagementSystem.Helpers.LoaiPhanQuyen.TuyChon))
                                            {
                                                <li>
                                                    <button type="button" class="dropdown-item text-warning" onclick="moHuyPhieuXuat(this, @item.ID)">
                                                        <i class="bi bi-x-circle me-2 text-warning"></i> Hủy phiếu
                                                    </button>
                                                </li>
                                            }
                                            
                                            @if (item.TrangThai == 1 && SalesManagementSystem.Helpers.PermissionHelper.HasPermission("PhieuXuatKho", SalesManagementSystem.Helpers.LoaiPhanQuyen.Xoa))
                                            {
                                                <li><hr class="dropdown-divider"></li>
                                                <li>
                                                    <button type="button" class="dropdown-item text-danger" onclick="deleteItemXuat(this, @item.ID)">
                                                        <i class="bi bi-trash-fill me-2 text-danger"></i> Xóa
                                                    </button>
                                                </li>
                                            }
                                        </ul>
                                    </div>
                                </td>
                                <td class="text-center align-middle">@(stt++)</td>
                                <td class="align-middle">@item.SoChungTu</td>
                                <td class="text-center align-middle">@(item.NgayXuat.ToString("dd/MM/yyyy"))</td>
                                <td class="align-middle">@item.TenKho</td>
                                <td class="align-middle">@item.TenNguoiNhan</td>
                                <td class="text-end align-middle">@(item.TongTienHang.ToString("N0"))</td>
                                <td class="text-end align-middle">@(item.TongTienThue.ToString("N0"))</td>
                                <td class="text-end align-middle">
                                    @item.TongCong.ToString("N0")
                                </td>
                                <td class="align-middle">@item.NguoiTaoText</td>
                                <td class="text-center align-middle">
                                    @(item.NgayTao?.ToString("dd/MM/yyyy HH:mm") ?? "")
                                </td>
                                <td class="text-center align-middle">
                                    @if (item.TrangThai == 1)
                                    {
                                        <span class="badge bg-secondary" style="font-size: 0.8rem; padding: 5px 8px;">Đề nghị xuất</span>
                                    }
                                    else if (item.TrangThai == 2)
                                    {
                                        <span class="badge bg-success" style="font-size: 0.8rem; padding: 5px 8px;">Đã xuất</span>
                                    }
                                    else if (item.TrangThai == 3)
                                    {
                                        <span class="badge bg-danger" style="font-size: 0.8rem; padding: 5px 8px;">Đã hủy</span>
                                    }
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="12" class="text-center py-4 text-muted">
                                <i class="bi bi-inbox fs-2 d-block mb-2"></i>
                                Không có dữ liệu
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    
    @Html.Partial("_Pagination", Model)
</div>
