@model SalesManagementSystem.Models.ViewModels.PagedListViewModel<SalesManagementSystem.Models.ViewModels.HaoHutHangHoaViewModel>
@{
    ViewBag.Title = "Quản lý hao hụt hàng hóa";
    var uid = Guid.NewGuid().ToString("N");
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" />

<style>
    #tblHaoHut th {
        position: relative;
        background-clip: padding-box;
    }
    #tblHaoHut th .resizer {
        position: absolute;
        top: 0;
        right: -3px;
        width: 6px;
        height: 100%;
        cursor: col-resize;
        user-select: none;
        z-index: 10;
        background-color: transparent;
    }
    #tblHaoHut th .resizer:hover, #tblHaoHut th .resizer.resizing {
        background-color: rgba(11, 91, 132, 0.5);
    }
</style>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0 fw-bold text-uppercase" style="color: #0b5b84; font-size: 1.25rem;">
        <i class="bi bi-boxes me-2"></i> @ViewBag.Title
    </h4>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-primary" style="background-color:#3498db;border-color:#3498db;" onclick="openCreateModal()">
            <i class="bi bi-plus-lg fw-bold"></i> Thêm mới
        </button>
    </div>
</div>

<div class="card shadow-sm mb-3">
    <div class="card-body py-2">
        <form id="searchForm" action="@Url.Action("GetList")" method="get">
            <div class="row gx-2 gy-2 align-items-end">
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Thời gian</label>
                    <select class="form-select form-select-sm ddl-thoi-gian">
                        <option value="current_month">Đầu tháng đến hiện tại</option>
                        <option value="month_1">Tháng 1</option>
                        <option value="month_2">Tháng 2</option>
                        <option value="month_3">Tháng 3</option>
                        <option value="month_4">Tháng 4</option>
                        <option value="month_5">Tháng 5</option>
                        <option value="month_6">Tháng 6</option>
                        <option value="month_7">Tháng 7</option>
                        <option value="month_8">Tháng 8</option>
                        <option value="month_9">Tháng 9</option>
                        <option value="month_10">Tháng 10</option>
                        <option value="month_11">Tháng 11</option>
                        <option value="month_12">Tháng 12</option>
                        <option value="quarter_1">Quý 1</option>
                        <option value="quarter_2">Quý 2</option>
                        <option value="quarter_3">Quý 3</option>
                        <option value="quarter_4">Quý 4</option>
                        <option value="current_year">Từ đầu năm đến hiện tại</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Từ ngày</label>
                    <input type="date" class="form-control form-control-sm" name="tuNgay" value="@ViewBag.TuNgay" />
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Đến ngày</label>
                    <input type="date" class="form-control form-control-sm" name="denNgay" value="@ViewBag.DenNgay" />
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Loại hao hụt</label>
                    <select class="form-select form-select-sm" name="loaiHaoHut">
                        <option value="0" @(ViewBag.LoaiHaoHut == 0 ? "selected" : "")>Tất cả</option>
                        <option value="1" @(ViewBag.LoaiHaoHut == 1 ? "selected" : "")>Hao hụt bán hàng</option>
                        <option value="2" @(ViewBag.LoaiHaoHut == 2 ? "selected" : "")>Hao hụt tồn kho</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Kho</label>
                    <select class="form-select form-select-sm select2" name="idKho">
                        <option value="0">Tất cả</option>
                        @if (ViewBag.KhoHangs != null)
                        {
                            foreach (var item in ViewBag.KhoHangs)
                            {
                                <option value="@item.ID" @(ViewBag.IDKho == item.ID ? "selected" : "")>@item.TenKhoHang</option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Số chứng từ</label>
                    <input type="text" class="form-control form-control-sm" name="soChungTu" placeholder="Nhập số CT..." value="@ViewBag.SoChungTu" />
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Sản phẩm</label>
                    <select class="form-select form-select-sm select2" name="idSanPham">
                        <option value="0">Tất cả</option>
                        @if (ViewBag.SanPhams != null)
                        {
                            foreach (var item in ViewBag.SanPhams)
                            {
                                <option value="@item.ID" @(ViewBag.IDSanPham == item.ID ? "selected" : "")>@item.TenSanPham</option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <label class="form-label text-muted small mb-1">Trạng thái</label>
                    <select class="form-select form-select-sm" name="trangThai">
                        <option value="0" @(ViewBag.TrangThai == 0 ? "selected" : "")>Tất cả</option>
                        <option value="1" @(ViewBag.TrangThai == 1 ? "selected" : "")>Lưu nháp</option>
                        <option value="2" @(ViewBag.TrangThai == 2 ? "selected" : "")>Đã ghi nhận</option>
                        <option value="3" @(ViewBag.TrangThai == 3 ? "selected" : "")>Đã hủy</option>
                    </select>
                </div>
                <div class="col-md-auto d-flex gap-2">
                    <button type="submit" class="btn btn-sm text-white px-3 shadow-sm"
                            style="background-color:#2998e4;border:none;">
                        <i class="bi bi-search"></i> Tìm kiếm
                    </button>
                    <button type="button" class="btn btn-sm text-white px-3 btn-reset shadow-sm"
                            style="background-color:#f1c40f;border:none;">
                        Làm mới
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

<div id="table-container">
    @Html.Partial("_HaoHutList", Model)
</div>

<!-- Modal container -->
<div id="modalContainer"></div>

@section Scripts {
    <script>
    <script>
        $(document).ready(function () {
            $('.select2').select2({ theme: 'bootstrap-5' });

            var $form = $('#searchForm');
            $('.btn-reset').on('click', function() {
                $form[0].reset();
                $form.find('select').val('0').trigger('change');
                $form.find('.ddl-thoi-gian').val('current_month');
                $form.find('input[name="tuNgay"]').val('@(new DateTime(DateTime.Now.Year, DateTime.Now.Month, 1).ToString("yyyy-MM-dd"))');
                $form.find('input[name="denNgay"]').val('@(DateTime.Now.ToString("yyyy-MM-dd"))');
                $form.trigger('submit');
            });
            
            // Xử lý thay đổi thời gian tương tự PhieuNhapKho
            var ddlThoiGian = $('.ddl-thoi-gian')[0];
            var txtTuNgay = $('input[name="tuNgay"]')[0];
            var txtDenNgay = $('input[name="denNgay"]')[0];
            
            function fDate(y, m, d) {
                var sm = m < 10 ? '0' + m : m;
                var sd = d < 10 ? '0' + d : d;
                return y + '-' + sm + '-' + sd;
            }

            function getDays(y, m) {
                var days = [0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
                if (m === 2 && ((y % 4 === 0 && y % 100 !== 0) || y % 400 === 0)) return 29;
                return days[m];
            }

            if (ddlThoiGian && txtTuNgay && txtDenNgay) {
                $(ddlThoiGian).on('change', function() {
                    var val = this.value;
                    if (!val) return;
                    
                    var now = new Date();
                    var y = now.getFullYear();
                    var s = '', e = '';
                    
                    if (val === 'current_year') {
                        s = fDate(y, 1, 1);
                        e = fDate(now.getFullYear(), now.getMonth() + 1, now.getDate());
                    } else if (val === 'current_month') {
                        var cm = now.getMonth() + 1;
                        s = fDate(y, cm, 1);
                        e = fDate(now.getFullYear(), now.getMonth() + 1, now.getDate());
                    } else if (val.indexOf('month_') === 0) {
                        var m = parseInt(val.replace('month_', ''), 10);
                        s = fDate(y, m, 1);
                        e = fDate(y, m, getDays(y, m));
                    } else if (val.indexOf('quarter_') === 0) {
                        var q = parseInt(val.replace('quarter_', ''), 10);
                        var startM = (q - 1) * 3 + 1;
                        var endM = startM + 2;
                        s = fDate(y, startM, 1);
                        e = fDate(y, endM, getDays(y, endM));
                    }

                    if (s && e) {
                        if (txtTuNgay._flatpickr) txtTuNgay._flatpickr.setDate(s);
                        else { $(txtTuNgay).val(s).trigger('change'); }
                        
                        if (txtDenNgay._flatpickr) txtDenNgay._flatpickr.setDate(e);
                        else { $(txtDenNgay).val(e).trigger('change'); }
                    }
                });
            }
        });

        var isOpeningModal = false;

        function disposeOldModal(modalId) {
            var oldModalEl = document.getElementById(modalId);
            if (oldModalEl) {
                var oldModal = bootstrap.Modal.getInstance(oldModalEl);
                if (oldModal) oldModal.dispose();
            }
        }

        function openCreateModal() {
            if (isOpeningModal) return;
            isOpeningModal = true;
            disposeOldModal('modalCreate');
            $.get('@Url.Action("_Create", "KHO_HaoHut")', function (html) {
                $('#modalContainer').html(html);
                var modal = new bootstrap.Modal(document.getElementById('modalCreate'));
                modal.show();
                isOpeningModal = false;
            }).fail(function() {
                isOpeningModal = false;
                showToast('error', 'Lỗi khi tải form thêm mới');
            });
        }

        function openEdit(id) {
            if (isOpeningModal) return;
            isOpeningModal = true;
            disposeOldModal('modalEdit');
            $.get('@Url.Action("_Edit", "KHO_HaoHut")?id=' + id, function (html) {
                $('#modalContainer').html(html);
                var modal = new bootstrap.Modal(document.getElementById('modalEdit'));
                modal.show();
                isOpeningModal = false;
            }).fail(function() {
                isOpeningModal = false;
                showToast('error', 'Lỗi khi tải form cập nhật');
            });
        }

        function openDetail(id) {
            if (isOpeningModal) return;
            isOpeningModal = true;
            disposeOldModal('modalDetail');
            $.get('@Url.Action("_Detail", "KHO_HaoHut")?id=' + id, function (html) {
                $('#modalContainer').html(html);
                var modal = new bootstrap.Modal(document.getElementById('modalDetail'));
                modal.show();
                isOpeningModal = false;
            }).fail(function() {
                isOpeningModal = false;
                showToast('error', 'Lỗi khi tải form chi tiết');
            });
        }

        function deleteHaoHut(id) {
            showConfirm('Bạn có chắc chắn muốn xóa phiếu hao hụt này? Dữ liệu sẽ bị xóa hoàn toàn.', function() {
                $.post('@Url.Action("Delete", "KHO_HaoHut")', { id: id }, function (res) {
                    if (res.success) {
                        showToast('success', 'Xóa thành công!');
                        $('#searchForm').trigger('submit');
                    } else {
                        showToast('error', res.message);
                    }
                });
            });
        }

        function cancelHaoHut(id) {
            showConfirm('Bạn có chắc chắn muốn hủy ghi nhận phiếu này? Dữ liệu tồn kho sẽ được khôi phục.', function() {
                $.post('@Url.Action("Huy", "KHO_HaoHut")', { id: id }, function (res) {
                    if (res.success) {
                        showToast('success', 'Hủy thành công!');
                        $('#searchForm').trigger('submit');
                    } else {
                        showToast('error', res.message);
                    }
                });
            });
        }

        function initTableResizer(tableId) {
            var table = document.getElementById(tableId);
            if (!table) return;
            
            var ths = table.querySelectorAll('th');
            ths.forEach(function(th) {
                if(th.innerText.trim() === '' && (th.style.width === '35px' || th.style.width === '40px' || th.style.width === '50px')) return;
                if(th.innerText.trim() === 'Thao tác' || th.innerText.trim() === 'STT') return;
                if(th.querySelector('.resizer')) return;

                var resizer = document.createElement('div');
                resizer.classList.add('resizer');
                th.appendChild(resizer);

                var startX, startWidth;
                
                resizer.addEventListener('mousedown', function(e) {
                    startX = e.pageX;
                    startWidth = th.offsetWidth;
                    resizer.classList.add('resizing');

                    var doDrag = function(e) {
                        var newWidth = startWidth + (e.pageX - startX);
                        if(newWidth > 30) {
                            th.style.width = newWidth + 'px';
                            th.style.minWidth = newWidth + 'px';
                            th.style.maxWidth = newWidth + 'px';
                        }
                    };

                    var stopDrag = function(e) {
                        resizer.classList.remove('resizing');
                        document.removeEventListener('mousemove', doDrag);
                        document.removeEventListener('mouseup', stopDrag);
                    };

                    document.addEventListener('mousemove', doDrag);
                    document.addEventListener('mouseup', stopDrag);
                    
                    e.preventDefault();
                });
            });
        }
    </script>
}
